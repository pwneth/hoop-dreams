import fs from 'fs';
import path from 'path';

/**
 * generate-apps-script.js
 * 
 * Standalone generator for the Google Apps Script backend.
 * Provides the full Apps Script logic as a template while requiring custom admin credentials.
 */

const args = process.argv.slice(2);
const adminUsername = args[0];
const adminPassword = args[1];

if (!adminUsername || !adminPassword) {
    console.error('Error: Admin username and password are required.');
    console.error('Usage: node generate-apps-script.js <admin_username> <admin_password>');
    process.exit(1);
}

const outputDir = path.resolve('google-apps-script');
const outputPath = path.join(outputDir, 'Code.gs');

if (!fs.existsSync(outputDir)) {
    fs.mkdirSync(outputDir, { recursive: true });
}

const lines = [
    "/**",
    " * Google Apps Script - HD Bets API",
    " * ",
    " * SETUP INSTRUCTIONS:",
    " * 1. Open Google Sheets",
    " * 2. Go to Extensions > Apps Script",
    " * 3. Delete any existing code and paste this entire file",
    " * 4. Run the 'setup' function once to initialize",
    " * 5. Click Deploy > New deployment > Web app > Execute as: Me > Who has access: Anyone",
    " * 6. Copy URL to your local .env file as VITE_APPS_SCRIPT_URL",
    " */",
    "",
    "const SHEET_BETS = 'Bets';",
    "const SHEET_USERS = 'Users';",
    "const SHEET_BRACKET = 'Bracket';",
    "const SHEET_BRACKET_PICKS = 'BracketPicks';",
    "const SHEET_BRACKET_CONFIG = 'BracketConfig';",
    "const SHEET_BRACKET_LOCKS = 'BracketLocks';",
    "",
    "/**",
    " * Setup function - RUN THIS ONCE to initialize Users sheet",
    " * After running, users can be added via the register endpoint or directly in the sheet.",
    " */",
    "function setup() {",
    "  const ss = SpreadsheetApp.getActiveSpreadsheet();",
    "  let usersSheet = ss.getSheetByName(SHEET_USERS);",
    "  ",
    "  if (!usersSheet) {",
    "    usersSheet = ss.insertSheet(SHEET_USERS);",
    "    usersSheet.appendRow(['Username', 'Password', 'IsAdmin']);",
    "  }",
    "}",
    "",
    "function hashPassword(password) {",
    "  const rawHash = Utilities.computeDigest(Utilities.DigestAlgorithm.SHA_256, password);",
    "  let txtHash = '';",
    "  for (let i = 0; i < rawHash.length; i++) {",
    "    let hashVal = rawHash[i];",
    "    if (hashVal < 0) {",
    "      hashVal += 256;",
    "    }",
    "    if (hashVal.toString(16).length == 1) {",
    "      txtHash += '0';",
    "    }",
    "    txtHash += hashVal.toString(16);",
    "  }",
    "  return txtHash;",
    "}",
    "",
    "/**",
    " * Handle GET requests",
    " */",
    "function doGet(e) {",
    "  return handleRequest(e);",
    "}",
    "",
    "/**",
    " * Handle POST requests",
    " */",
    "function doPost(e) {",
    "  return handleRequest(e);",
    "}",
    "",
    "function handleRequest(e) {",
    "  try {",
    "    const params = e.parameter || {};",
    "    // Handle POST body if present",
    "    if (e.postData && e.postData.contents) {",
    "      try {",
    "        const body = JSON.parse(e.postData.contents);",
    "        Object.assign(params, body);",
    "      } catch (err) {",
    "        // ignore",
    "      }",
    "    }",
    "",
    "    const action = params.action;",
    "",
    "    if (action === 'login') {",
    "      return loginUser(params.username, params.password);",
    "    }",
    "    ",
    "    if (action === 'register') {",
    "      return registerUser(params.username, params.password);",
    "    }",
    "",
    "    if (action === 'changePassword') {",
    "      return changePassword(params.username, params.oldPassword, params.newPassword);",
    "    }",
    "    ",
    "    // Authenticate validation for other actions",
    "    const user = authenticate(params.username, params.password);",
    "    if (!user) {",
    "      return createResponse({ success: false, error: 'Authentication failed' });",
    "    }",
    "",
    "    const ss = SpreadsheetApp.getActiveSpreadsheet();",
    "    const sheet = ss.getSheetByName(SHEET_BETS) || ss.getSheets()[0];",
    "",
    "    if (action === 'getBets') {",
    "      return getBets(sheet);",
    "    }",
    "    ",
    "    if (action === 'addBet') {",
    "      return addBet(sheet, params, user);",
    "    }",
    "",
    "    if (action === 'updateBet') {",
    "      return updateBet(sheet, params, user);",
    "    }",
    "",
    "    if (action === 'confirmBet') {",
    "      return confirmBet(sheet, params, user);",
    "    }",
    "",
    "    if (action === 'markPaid') {",
    "      return markPaid(sheet, params, user);",
    "    }",
    "",
    "    if (action === 'getUsers') {",
    "      return getUsers();",
    "    }",
    "",
    "    if (action === 'getBracket') {",
    "      return getBracket(ss, user);",
    "    }",
    "",
    "    if (action === 'submitPick') {",
    "      return submitPick(ss, params, user);",
    "    }",
    "",
    "    if (action === 'updateMatchup') {",
    "      return updateMatchup(ss, params, user);",
    "    }",
    "",
    "    if (action === 'setBracketConfig') {",
    "      return setBracketConfig(ss, params, user);",
    "    }",
    "",
    "    if (action === 'lockPicks') {",
    "      return lockPicks(ss, user);",
    "    }",
    "    ",
    "    // Default to getBets if no action (backward compatibility-ish)",
    "    return getBets(sheet);",
    "    ",
    "  } catch (error) {",
    "    return createResponse({ success: false, error: error.message });",
    "  }",
    "}",
    "",
    "function authenticate(username, password) {",
    "  if (!username || !password) return null;",
    "  const ss = SpreadsheetApp.getActiveSpreadsheet();",
    "  const usersSheet = ss.getSheetByName(SHEET_USERS);",
    "  if (!usersSheet) return null;",
    "  ",
    "  const data = usersSheet.getDataRange().getValues();",
    "  // Skip header",
    "  for (let i = 1; i < data.length; i++) {",
    "    const row = data[i];",
    "    if (row[0].toLowerCase() === username.toLowerCase()) {",
    "      const hashedPassword = hashPassword(password);",
    "      if (row[1] === hashedPassword) {",
    "        return { username: row[0], isAdmin: row[2] === true || row[2] === 'true' };",
    "      }",
    "    }",
    "  }",
    "  return null;",
    "}",
    "",
    "function loginUser(username, password) {",
    "  const user = authenticate(username, password);",
    "  if (user) {",
    "    return createResponse({ success: true, user: user });",
    "  }",
    "  return createResponse({ success: false, error: 'Invalid username or password' });",
    "}",
    "",
    "function registerUser(username, password) {",
    "  const ss = SpreadsheetApp.getActiveSpreadsheet();",
    "  let usersSheet = ss.getSheetByName(SHEET_USERS);",
    "  if (!usersSheet) return createResponse({ success: false, error: 'System not initialized' });",
    "  ",
    "  // Check existing",
    "  const data = usersSheet.getDataRange().getValues();",
    "  for (let i = 1; i < data.length; i++) {",
    "    if (data[i][0].toLowerCase() === username.toLowerCase()) {",
    "      return createResponse({ success: false, error: 'Username already exists' });",
    "    }",
    "  }",
    "  ",
    "  usersSheet.appendRow([username, hashPassword(password), false]);",
    "  return createResponse({ success: true, user: { username: username, isAdmin: false } });",
    "}",
    "",
    "function changePassword(username, oldPassword, newPassword) {",
    "  const ss = SpreadsheetApp.getActiveSpreadsheet();",
    "  const usersSheet = ss.getSheetByName(SHEET_USERS);",
    "  ",
    "  const data = usersSheet.getDataRange().getValues();",
    "  for (let i = 1; i < data.length; i++) {",
    "    if (data[i][0].toLowerCase() === username.toLowerCase()) {",
    "      const currentHash = data[i][1];",
    "      if (currentHash === hashPassword(oldPassword)) {",
    "        usersSheet.getRange(i + 1, 2).setValue(hashPassword(newPassword));",
    "        return createResponse({ success: true, message: 'Password updated' });",
    "      } else {",
    "        return createResponse({ success: false, error: 'Incorrect old password' });",
    "      }",
    "    }",
    "  }",
    "  return createResponse({ success: false, error: 'User not found' });",
    "}",
    "",
    "function getBets(sheet) {",
    "  const lastRow = sheet.getLastRow();",
    "  if (lastRow < 2) return createResponse({ success: true, data: [] });",
    "",
    "  // Read up to column 17 (Q) to include proposal fields",
    "  const data = sheet.getRange(2, 1, lastRow - 1, 17).getValues();",
    "  ",
    "  const rows = data.map((row, index) => {",
    "    return {",
    "      id: index,",
    "      Date: row[0],",
    "      Better1: row[1],",
    "      Better2: row[2],",
    "      Better1Bet: row[3],",
    "      Better2Bet: row[4],",
    "      Better1Reward: row[5],",
    "      Better2Reward: row[6],",
    "      WinnerLabel: row[7],",
    "      Status: row[8],",
    "      proposerWinner: row[13] || null,",
    "      proposedWinnerValue: row[14] || null,",
    "      proposerPaid: row[15] || null,",
    "      proposedPaidValue: row[16] || null",
    "    };",
    "  });",
    "  ",
    "  return createResponse({ success: true, data: rows });",
    "}",
    "",
    "function addBet(sheet, params, user) {",
    "  const better1 = params.better1;",
    "  const better2 = params.better2;",
    "  ",
    "  if (!user.isAdmin && user.username !== better1 && user.username !== better2) {",
    "    return createResponse({ success: false, error: 'You can only place bets for yourself' });",
    "  }",
    "",
    "  const date = new Date();",
    "  const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];",
    "  const dateStr = date.getDate() + '-' + months[date.getMonth()] + '-' + date.getFullYear();",
    "  ",
    "  const newRow = [",
    "    dateStr,",
    "    better1 || '',",
    "    better2 || '',",
    "    params.better1Bet || '',",
    "    params.better2Bet || '',",
    "    '€' + parseFloat(params.better1Reward || 0).toFixed(2),",
    "    '€' + parseFloat(params.better2Reward || 0).toFixed(2),",
    "    '', // WinnerLabel",
    "    'Pending Confirmation', // Status",
    "    '', // WinnerName",
    "    '', // AmountWon",
    "    '', // LoserName",
    "    '', // AmountLost",
    "    '', // ProposerWinner",
    "    '', // ProposedWinnerValue",
    "    '', // ProposerPaid",
    "    ''  // ProposedPaidValue",
    "  ];",
    "  ",
    "  sheet.appendRow(newRow);",
    "  SpreadsheetApp.flush();",
    "  return createResponse({ success: true, message: 'Bet added' });",
    "}",
    "",
    "function updateBet(sheet, params, user) {",
    "  const rowIndex = parseInt(params.rowId) + 2;",
    "  const winnerKey = params.winner;",
    "  ",
    "  const rowRange = sheet.getRange(rowIndex, 1, 1, 17);",
    "  const rowValues = rowRange.getValues()[0];",
    "  ",
    "  const better1 = rowValues[1];",
    "  const better2 = rowValues[2];",
    "  ",
    "  if (!user.isAdmin && user.username !== better1 && user.username !== better2) {",
    "    return createResponse({ success: false, error: 'Only participants can update this bet' });",
    "  }",
    "  ",
    "  const existingProposer = rowValues[13];",
    "  const existingProposal = rowValues[14];",
    "  ",
    "  const isConceding = (winnerKey.toLowerCase() === 'better1' && user.username === better2) || ",
    "                      (winnerKey.toLowerCase() === 'better2' && user.username === better1);",
    "  ",
    "  if (user.isAdmin || isConceding || (existingProposer && existingProposer !== user.username && existingProposal === winnerKey)) {",
    "    const fullRow = rowValues;",
    "    let winnerName, loserName, amountWon, amountLost;",
    "   ",
    "    if (winnerKey === 'better1' || winnerKey === 'Better 1') {",
    "      winnerName = fullRow[1];",
    "      loserName = fullRow[2];",
    "      amountWon = fullRow[5];",
    "      amountLost = fullRow[6];",
    "    } else {",
    "      winnerName = fullRow[2];",
    "      loserName = fullRow[1];",
    "      amountWon = fullRow[6];",
    "      amountLost = fullRow[5];",
    "    }",
    "  ",
    "    const winnerLabel = (winnerKey === 'better1' || winnerKey === 'Better 1') ? 'Better 1' : 'Better 2';",
    "    ",
    "    sheet.getRange(rowIndex, 8).setValue(winnerLabel);",
    "    sheet.getRange(rowIndex, 9).setValue('Unpaid');",
    "    sheet.getRange(rowIndex, 10).setValue(winnerName);",
    "    sheet.getRange(rowIndex, 11).setValue(amountWon);",
    "    sheet.getRange(rowIndex, 12).setValue(loserName);",
    "    sheet.getRange(rowIndex, 13).setValue(amountLost);",
    "    ",
    "    sheet.getRange(rowIndex, 14).setValue('');",
    "    sheet.getRange(rowIndex, 15).setValue('');",
    "    ",
    "    SpreadsheetApp.flush();",
    "    return createResponse({ success: true, message: 'Winner confirmed!' });",
    "  } ",
    "  else {",
    "    sheet.getRange(rowIndex, 14).setValue(user.username);",
    "    sheet.getRange(rowIndex, 15).setValue(winnerKey);",
    "    SpreadsheetApp.flush();",
    "    return createResponse({ success: true, message: 'Winner proposed. Waiting for confirmation.' });",
    "  }",
    "}",
    "",
    "function markPaid(sheet, params, user) {",
    "  const rowIndex = parseInt(params.rowId) + 2;",
    "  const rowRange = sheet.getRange(rowIndex, 1, 1, 17);",
    "  const rowValues = rowRange.getValues()[0];",
    "  ",
    "  const better1 = rowValues[1];",
    "  const better2 = rowValues[2];",
    "  ",
    "  if (!user.isAdmin && user.username !== better1 && user.username !== better2) {",
    "    return createResponse({ success: false, error: 'Only participants can update this bet' });",
    "  }",
    "",
    "  const winnerName = rowValues[9];",
    "",
    "  if (user.username === winnerName) {",
    "    sheet.getRange(rowIndex, 9).setValue('Paid');",
    "    sheet.getRange(rowIndex, 16).setValue('');",
    "    sheet.getRange(rowIndex, 17).setValue('');",
    "    SpreadsheetApp.flush();",
    "    return createResponse({ success: true, message: 'Payment confirmed!' });",
    "  }",
    "",
    "  const existingProposer = rowValues[15];",
    "  const existingProposal = rowValues[16];",
    "",
    "  if (existingProposer && existingProposer !== user.username) {",
    "    sheet.getRange(rowIndex, 9).setValue('Paid'); ",
    "    sheet.getRange(rowIndex, 16).setValue('');",
    "    sheet.getRange(rowIndex, 17).setValue('');",
    "    SpreadsheetApp.flush();",
    "    return createResponse({ success: true, message: 'Payment confirmed!' });",
    "  } else {",
    "    sheet.getRange(rowIndex, 16).setValue(user.username);",
    "    sheet.getRange(rowIndex, 17).setValue('Paid');",
    "    SpreadsheetApp.flush();",
    "    return createResponse({ success: true, message: 'Payment marked. Waiting for winner to confirm receipt.' });",
    "  }",
    "}",
    "",
    "function getUsers() {",
    "  const ss = SpreadsheetApp.getActiveSpreadsheet();",
    "  const usersSheet = ss.getSheetByName(SHEET_USERS);",
    "  if (!usersSheet) return createResponse({ success: true, data: [] });",
    "  ",
    "  const data = usersSheet.getDataRange().getValues();",
    "  const users = data.slice(1)",
    "    .map(row => row[0])",
    "    .filter(name => name && name.toLowerCase() !== 'admin');",
    "    ",
    "  return createResponse({ success: true, data: users });",
    "}",
    "",
    "function confirmBet(sheet, params, user) {",
    "  const rowIndex = parseInt(params.rowId) + 2;",
    "  const confirmAction = params.confirmAction;",
    "  const rowRange = sheet.getRange(rowIndex, 1, 1, 17);",
    "  const rowValues = rowRange.getValues()[0];",
    "  const better2 = rowValues[2];",
    "  const currentStatus = rowValues[8];",
    "",
    "  if (currentStatus !== 'Pending Confirmation') {",
    "    return createResponse({ success: false, error: 'Bet is already processed' });",
    "  }",
    "",
    "  if (!user.isAdmin && user.username !== better2) {",
    "    return createResponse({ success: false, error: 'Only the opponent can confirm this bet' });",
    "  }",
    "",
    "  if (confirmAction === 'confirm') {",
    "    sheet.getRange(rowIndex, 9).setValue('');",
    "  } else {",
    "    sheet.deleteRow(rowIndex);",
    "  }",
    "",
    "  SpreadsheetApp.flush();",
    "  return createResponse({ success: true, message: 'Bet ' + confirmAction + 'ed' });",
    "}",
    "",
    "// ============================================",
    "// BRACKET",
    "// ============================================",
    "",
    "function setupBracket() {",
    "  const ss = SpreadsheetApp.getActiveSpreadsheet();",
    "",
    "  let bracketSheet = ss.getSheetByName(SHEET_BRACKET);",
    "  if (!bracketSheet) {",
    "    bracketSheet = ss.insertSheet(SHEET_BRACKET);",
    "    bracketSheet.appendRow(['MatchupId', 'Round', 'Conference', 'SeedTop', 'SeedBottom', 'TeamTop', 'TeamBottom', 'Winner', 'SeriesScore', 'Status', 'GamesPlayed']);",
    "",
    "    const matchups = [",
    "      ['west_playin_1', 'playin', 'west', 7, 8, '', '', '', '', 'upcoming', ''],",
    "      ['west_playin_2', 'playin', 'west', 9, 10, '', '', '', '', 'upcoming', ''],",
    "      ['west_playin_3', 'playin', 'west', '', '', '', '', '', '', 'upcoming', ''],",
    "      ['east_playin_1', 'playin', 'east', 7, 8, '', '', '', '', 'upcoming', ''],",
    "      ['east_playin_2', 'playin', 'east', 9, 10, '', '', '', '', 'upcoming', ''],",
    "      ['east_playin_3', 'playin', 'east', '', '', '', '', '', '', 'upcoming', ''],",
    "      ['west_r1_1', 'r1', 'west', 1, 8, '', '', '', '', 'upcoming', ''],",
    "      ['west_r1_2', 'r1', 'west', 4, 5, '', '', '', '', 'upcoming', ''],",
    "      ['west_r1_3', 'r1', 'west', 3, 6, '', '', '', '', 'upcoming', ''],",
    "      ['west_r1_4', 'r1', 'west', 2, 7, '', '', '', '', 'upcoming', ''],",
    "      ['east_r1_1', 'r1', 'east', 1, 8, '', '', '', '', 'upcoming', ''],",
    "      ['east_r1_2', 'r1', 'east', 4, 5, '', '', '', '', 'upcoming', ''],",
    "      ['east_r1_3', 'r1', 'east', 3, 6, '', '', '', '', 'upcoming', ''],",
    "      ['east_r1_4', 'r1', 'east', 2, 7, '', '', '', '', 'upcoming', ''],",
    "      ['west_r2_1', 'r2', 'west', '', '', '', '', '', '', 'upcoming', ''],",
    "      ['west_r2_2', 'r2', 'west', '', '', '', '', '', '', 'upcoming', ''],",
    "      ['east_r2_1', 'r2', 'east', '', '', '', '', '', '', 'upcoming', ''],",
    "      ['east_r2_2', 'r2', 'east', '', '', '', '', '', '', 'upcoming', ''],",
    "      ['west_r3_1', 'r3', 'west', '', '', '', '', '', '', 'upcoming', ''],",
    "      ['east_r3_1', 'r3', 'east', '', '', '', '', '', '', 'upcoming', ''],",
    "      ['finals', 'finals', '', '', '', '', '', '', '', 'upcoming', ''],",
    "    ];",
    "",
    "    matchups.forEach(row => bracketSheet.appendRow(row));",
    "  }",
    "",
    "  let picksSheet = ss.getSheetByName(SHEET_BRACKET_PICKS);",
    "  if (!picksSheet) {",
    "    picksSheet = ss.insertSheet(SHEET_BRACKET_PICKS);",
    "    picksSheet.appendRow(['MatchupId', 'Username', 'Pick', 'Games', 'Timestamp']);",
    "  }",
    "",
    "  let configSheet = ss.getSheetByName(SHEET_BRACKET_CONFIG);",
    "  if (!configSheet) {",
    "    configSheet = ss.insertSheet(SHEET_BRACKET_CONFIG);",
    "    configSheet.appendRow(['Key', 'Value']);",
    "    configSheet.appendRow(['buyIn', 0]);",
    "  }",
    "",
    "  let locksSheet = ss.getSheetByName(SHEET_BRACKET_LOCKS);",
    "  if (!locksSheet) {",
    "    locksSheet = ss.insertSheet(SHEET_BRACKET_LOCKS);",
    "    locksSheet.appendRow(['Username', 'LockedAt']);",
    "  }",
    "}",
    "",
    "function getBracket(ss, user) {",
    "  const bracketSheet = ss.getSheetByName(SHEET_BRACKET);",
    "  if (!bracketSheet || bracketSheet.getLastRow() < 2) {",
    "    return createResponse({ success: true, matchups: [], picks: [], scores: [], buyIn: 0, locked: false });",
    "  }",
    "",
    "  const data = bracketSheet.getRange(2, 1, bracketSheet.getLastRow() - 1, 11).getValues();",
    "  const matchups = data.map(row => ({",
    "    matchupId: row[0],",
    "    round: row[1],",
    "    conference: row[2],",
    "    seedTop: row[3] !== '' ? row[3] : null,",
    "    seedBottom: row[4] !== '' ? row[4] : null,",
    "    teamTop: row[5] || '',",
    "    teamBottom: row[6] || '',",
    "    winner: row[7] || '',",
    "    seriesScore: row[8] || '',",
    "    status: row[9] || 'upcoming',",
    "    gamesPlayed: row[10] !== '' ? Number(row[10]) : null",
    "  }));",
    "",
    "  const picksSheet = ss.getSheetByName(SHEET_BRACKET_PICKS);",
    "  let allPicks = [];",
    "  let userPicks = [];",
    "  if (picksSheet && picksSheet.getLastRow() >= 2) {",
    "    const picksData = picksSheet.getRange(2, 1, picksSheet.getLastRow() - 1, 4).getValues();",
    "    allPicks = picksData.map(row => ({",
    "      matchupId: row[0],",
    "      username: row[1],",
    "      pick: row[2],",
    "      games: row[3] !== '' ? Number(row[3]) : null",
    "    }));",
    "    userPicks = allPicks",
    "      .filter(p => p.username.toLowerCase() === user.username.toLowerCase())",
    "      .map(p => ({ matchupId: p.matchupId, pick: p.pick, games: p.games }));",
    "  }",
    "",
    "  const completedMatchups = matchups.filter(m => m.winner !== '');",
    "  const userScores = {};",
    "  allPicks.forEach(p => {",
    "    if (!userScores[p.username]) {",
    "      userScores[p.username] = { username: p.username, points: 0, correctPicks: 0, correctGames: 0, totalPicks: 0 };",
    "    }",
    "    userScores[p.username].totalPicks++;",
    "    const matchup = completedMatchups.find(m => m.matchupId === p.matchupId);",
    "    if (matchup) {",
    "      if (p.pick === matchup.winner) {",
    "        userScores[p.username].correctPicks++;",
    "        userScores[p.username].points += 1;",
    "        if (matchup.round !== 'playin' && matchup.gamesPlayed && p.games === matchup.gamesPlayed) {",
    "          userScores[p.username].correctGames++;",
    "          userScores[p.username].points += 1;",
    "        }",
    "      }",
    "    }",
    "  });",
    "  const scores = Object.values(userScores).sort((a, b) => b.points - a.points);",
    "",
    "  let buyIn = 0;",
    "  const configSheet = ss.getSheetByName(SHEET_BRACKET_CONFIG);",
    "  if (configSheet && configSheet.getLastRow() >= 2) {",
    "    const configData = configSheet.getRange(2, 1, configSheet.getLastRow() - 1, 2).getValues();",
    "    const buyInRow = configData.find(row => row[0] === 'buyIn');",
    "    if (buyInRow) buyIn = Number(buyInRow[1]) || 0;",
    "  }",
    "",
    "  let locked = false;",
    "  const locksSheet = ss.getSheetByName(SHEET_BRACKET_LOCKS);",
    "  if (locksSheet && locksSheet.getLastRow() >= 2) {",
    "    const locksData = locksSheet.getRange(2, 1, locksSheet.getLastRow() - 1, 1).getValues();",
    "    locked = locksData.some(row => row[0].toLowerCase() === user.username.toLowerCase());",
    "  }",
    "",
    "  return createResponse({ success: true, matchups: matchups, picks: userPicks, scores: scores, buyIn: buyIn, locked: locked });",
    "}",
    "",
    "function submitPick(ss, params, user) {",
    "  const matchupId = params.matchupId;",
    "  const pick = params.pick;",
    "  const games = params.games || '';",
    "",
    "  if (!matchupId || !pick) {",
    "    return createResponse({ success: false, error: 'Missing matchupId or pick' });",
    "  }",
    "",
    "  const locksSheet = ss.getSheetByName(SHEET_BRACKET_LOCKS);",
    "  if (locksSheet && locksSheet.getLastRow() >= 2) {",
    "    const locksData = locksSheet.getRange(2, 1, locksSheet.getLastRow() - 1, 1).getValues();",
    "    if (locksData.some(row => row[0].toLowerCase() === user.username.toLowerCase())) {",
    "      return createResponse({ success: false, error: 'Your picks are locked and cannot be changed' });",
    "    }",
    "  }",
    "",
    "  const picksSheet = ss.getSheetByName(SHEET_BRACKET_PICKS);",
    "  if (!picksSheet) {",
    "    return createResponse({ success: false, error: 'BracketPicks sheet not found. Run setupBracket().' });",
    "  }",
    "",
    "  if (picksSheet.getLastRow() >= 2) {",
    "    const picksData = picksSheet.getRange(2, 1, picksSheet.getLastRow() - 1, 5).getValues();",
    "    for (let i = 0; i < picksData.length; i++) {",
    "      if (picksData[i][0] === matchupId && picksData[i][1].toLowerCase() === user.username.toLowerCase()) {",
    "        picksSheet.getRange(i + 2, 3).setValue(pick);",
    "        picksSheet.getRange(i + 2, 4).setValue(games);",
    "        picksSheet.getRange(i + 2, 5).setValue(new Date());",
    "        SpreadsheetApp.flush();",
    "        return createResponse({ success: true, message: 'Pick updated' });",
    "      }",
    "    }",
    "  }",
    "",
    "  picksSheet.appendRow([matchupId, user.username, pick, games, new Date()]);",
    "  SpreadsheetApp.flush();",
    "  return createResponse({ success: true, message: 'Pick saved' });",
    "}",
    "",
    "function updateMatchup(ss, params, user) {",
    "  if (!user.isAdmin) {",
    "    return createResponse({ success: false, error: 'Admin access required' });",
    "  }",
    "",
    "  const matchupId = params.matchupId;",
    "  if (!matchupId) {",
    "    return createResponse({ success: false, error: 'Missing matchupId' });",
    "  }",
    "",
    "  const bracketSheet = ss.getSheetByName(SHEET_BRACKET);",
    "  if (!bracketSheet) {",
    "    return createResponse({ success: false, error: 'Bracket sheet not found. Run setupBracket().' });",
    "  }",
    "",
    "  const data = bracketSheet.getRange(2, 1, bracketSheet.getLastRow() - 1, 11).getValues();",
    "  let rowIndex = -1;",
    "  for (let i = 0; i < data.length; i++) {",
    "    if (data[i][0] === matchupId) {",
    "      rowIndex = i + 2;",
    "      break;",
    "    }",
    "  }",
    "",
    "  if (rowIndex === -1) {",
    "    return createResponse({ success: false, error: 'Matchup not found' });",
    "  }",
    "",
    "  if (params.teamTop !== undefined) {",
    "    bracketSheet.getRange(rowIndex, 6).setValue(params.teamTop);",
    "  }",
    "  if (params.teamBottom !== undefined) {",
    "    bracketSheet.getRange(rowIndex, 7).setValue(params.teamBottom);",
    "  }",
    "  if (params.winner !== undefined) {",
    "    bracketSheet.getRange(rowIndex, 8).setValue(params.winner);",
    "    bracketSheet.getRange(rowIndex, 10).setValue(params.winner ? 'completed' : 'upcoming');",
    "  }",
    "  if (params.seriesScore !== undefined) {",
    "    bracketSheet.getRange(rowIndex, 9).setValue(params.seriesScore);",
    "  }",
    "  if (params.status !== undefined) {",
    "    bracketSheet.getRange(rowIndex, 10).setValue(params.status);",
    "  }",
    "  if (params.gamesPlayed !== undefined) {",
    "    bracketSheet.getRange(rowIndex, 11).setValue(params.gamesPlayed);",
    "  }",
    "",
    "  SpreadsheetApp.flush();",
    "  return createResponse({ success: true, message: 'Matchup updated' });",
    "}",
    "",
    "function setBracketConfig(ss, params, user) {",
    "  if (!user.isAdmin) {",
    "    return createResponse({ success: false, error: 'Admin access required' });",
    "  }",
    "",
    "  const configSheet = ss.getSheetByName(SHEET_BRACKET_CONFIG);",
    "  if (!configSheet) {",
    "    return createResponse({ success: false, error: 'BracketConfig sheet not found. Run setupBracket().' });",
    "  }",
    "",
    "  if (params.buyIn !== undefined) {",
    "    const data = configSheet.getRange(2, 1, configSheet.getLastRow() - 1, 2).getValues();",
    "    let found = false;",
    "    for (let i = 0; i < data.length; i++) {",
    "      if (data[i][0] === 'buyIn') {",
    "        configSheet.getRange(i + 2, 2).setValue(Number(params.buyIn));",
    "        found = true;",
    "        break;",
    "      }",
    "    }",
    "    if (!found) {",
    "      configSheet.appendRow(['buyIn', Number(params.buyIn)]);",
    "    }",
    "  }",
    "",
    "  SpreadsheetApp.flush();",
    "  return createResponse({ success: true, message: 'Config updated' });",
    "}",
    "",
    "function lockPicks(ss, user) {",
    "  const locksSheet = ss.getSheetByName(SHEET_BRACKET_LOCKS);",
    "  if (!locksSheet) {",
    "    return createResponse({ success: false, error: 'BracketLocks sheet not found. Run setupBracket().' });",
    "  }",
    "",
    "  if (locksSheet.getLastRow() >= 2) {",
    "    const locksData = locksSheet.getRange(2, 1, locksSheet.getLastRow() - 1, 1).getValues();",
    "    if (locksData.some(row => row[0].toLowerCase() === user.username.toLowerCase())) {",
    "      return createResponse({ success: false, error: 'Picks are already locked' });",
    "    }",
    "  }",
    "",
    "  locksSheet.appendRow([user.username, new Date()]);",
    "  SpreadsheetApp.flush();",
    "  return createResponse({ success: true, message: 'Picks locked!' });",
    "}",
    "",
    "function createResponse(data) {",
    "  return ContentService.createTextOutput(JSON.stringify(data))",
    "    .setMimeType(ContentService.MimeType.JSON);",
    "}"
];

fs.writeFileSync(outputPath, lines.join('\\n'));
console.log('Successfully generated Code.gs at ' + outputPath);
console.log('Admin User: ' + adminUsername);
